{
  "name": "weather",
  "version": "1.0.0",
  "description": "Example MCP plugin — get current weather for a city.",
  "command": "python",
  "args": ["server.py"],
  "tools": [
    {
      "name": "get_weather",
      "description": "Get the current weather for a city (demo — returns mock data).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "city": {
            "type": "string",
            "description": "City name"
          }
        },
        "required": ["city"]
      }
    }
  ]
}
